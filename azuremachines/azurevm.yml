---
- name: "Create Azure VM"
  hosts: "all"
  become: true
  collections:
    - azure.azcollection
  roles:
    - { role: setup }
- name: "Setup VM"    
  hosts: "{{ output_ip_address.state.ip_address }}"
  become: true
  roles:
    - { role: hardening }
